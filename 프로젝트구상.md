
mssql 쿼리의 select 절에 있는 @변수를 필드의 alias 로 변환해 주는 정적인 웹사이트.

## MSSQL 쿼리 변환 웹유틸 배포 방법
### 저장소 생성

예: sql-alias-tool

소스코드(HTML/JS) 업로드

Pages 설정

저장소 → Settings → Pages → Branch 선택 (예: main)

자동으로 https://codeontech.github.io/sql-alias-tool 주소가 생성됨

웹앱 구현

JavaScript로 MSSQL 쿼리 파서를 작성해 @변수 = 컬럼 → 컬럼 AS alias 변환 기능 제공

브라우저에서 실행되므로 서버 비용 없음


### 구현 방식
HTML: 입력창(Textarea)과 결과창을 배치

JavaScript:

사용자가 붙여넣은 SQL 텍스트를 읽음

정규식으로 @변수 = 컬럼 패턴을 찾아서 컬럼 AS alias로 치환

결과를 출력창에 표시

CSS: 간단한 UI 스타일링


나의 깃헙 계정은 whwnddml 이지만, 별도 그룹으로 codeontech 을 가지고 있음.

